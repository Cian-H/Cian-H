<svg viewBox="0 0 800 220" xmlns="http://www.w3.org/2000/svg">
  <style>
    .mobile { display: none; }
    .desktop { display: block; }

    text {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
      fill: #333;
      font-size: 18px;
      font-weight: bold;
    }

    @media (prefers-color-scheme: dark) {
      text { fill: #e6edf3; }
    }

    @media (max-width: 600px) {
      .desktop { display: none; }
      .mobile { display: block; }
    }
  </style>

  <g class="desktop">
    <image href="{{ python }}" x="0" y="40" height="40" width="40"/>
    <image href="{{ rust }}" x="50" y="40" height="40" width="40"/>
    <image href="{{ julia }}" x="0" y="90" height="40" width="40"/>
    <image href="{{ elixir }}" x="50" y="90" height="40" width="40"/>
    <image href="{{ numpy }}" x="0" y="140" height="40" width="40"/>
    <image href="{{ pandas }}" x="50" y="140" height="40" width="40"/>

    <text x="350" y="110" text-anchor="middle">Whitespace is important...</text>

    <image href="{{ bongo }}" x="580" y="10" height="180" width="180"/>
  </g>

  <g class="mobile">
    <image href="{{ bongo }}" x="310" y="0" height="150" width="150"/>

    <text x="400" y="170" text-anchor="middle">Whitespace is important...</text>

    <image href="{{ python }}" x="320" y="190" height="30" width="30"/>
    <image href="{{ rust }}" x="360" y="190" height="30" width="30"/>
    <image href="{{ julia }}" x="400" y="190" height="30" width="30"/>
    <image href="{{ elixir }}" x="440" y="190" height="30" width="30"/>
  </g>
</svg>
